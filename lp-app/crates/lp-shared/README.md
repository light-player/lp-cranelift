# lp-shared

Shared lightplayer code for use by `lp-engine`, `lp-server` and other embeddable portions of Lightplayer.

`no_std`, desinged for running on embedded devices.

Contains the abstractions, config, state, and other shared code used by engine and clients.