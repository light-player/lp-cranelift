test compile
target riscv32

function %sshr_i32(i32, i32) -> i32 {
block0(v0: i32, v1: i32):
    v2 = sshr v0, v1
    return v2
}

; Verify we use SRA (shift right arithmetic) instruction
; check: sra
; check-NOT: sraw

