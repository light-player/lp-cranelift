// test run
// test fixed32

float main() {
    float sum = 0.0;
    float val = 0.0625;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    sum = sum + val;
    return sum;
}

// run: ~= 1.0

