# Plan Phases

1. Add config fields to node runtimes
2. Update node runtime init() methods to store configs
3. Add config() getter methods to node runtimes
4. Update render_textures_panel() to use render_texture()
5. Update render_shaders_panel() to use render_shader_panel()
6. Update render_fixtures_panel() to use render_fixture()
7. Remove #[allow(dead_code)] from helper functions
8. Cleanup and testing

